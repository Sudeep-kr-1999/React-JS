import React from 'react'

function FragmentDemo() {
    return (
        <React.Fragment>
            <h1>FragmentDemo</h1>
            <p>This descibe the Fragment Demo Component</p>
        </React.Fragment>
    )
}

export default FragmentDemo;

// React.Fragment:--- it help us to wrap all JSX tags into a single bundle type we can say and oppose us to use one extra div tag 

                    //   it can also accept key attribute when rendering list of items 